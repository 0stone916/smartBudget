spring:
  datasource:
    driver-class-name: com.mysql.cj.jdbc.Driver
    url: jdbc:mysql://localhost:3306/smartbudget_test?serverTimezone=Asia/Seoul
    username: root
    password: 3152

  batch:
    jdbc:
      initialize-schema: always
    job:
      enabled: false
